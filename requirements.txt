# --- 핵심 ---
anthropic>=0.40.0          # Claude API (텍스트/사진 분석, 점수화)
pydantic>=2.7              # 데이터 모델 검증
pydantic-settings>=2.3     # .env 기반 설정 로딩
pyyaml>=6.0                # config 로딩
httpx>=0.27               # HTTP 클라이언트 (수집기)
tenacity>=8.3              # 재시도/백오프
typer>=0.12               # CLI

# --- 수집 / 파싱 ---
beautifulsoup4>=4.12       # HTML 파싱 (블로그/웹사이트)
lxml>=5.2

# --- 출력 ---
openpyxl>=3.1             # 엑셀 출력
gspread>=6.1             # 구글시트 출력 (선택)
google-auth>=2.30        # 구글시트 인증 (선택)

# --- 개발 ---
pytest>=8.2
ruff>=0.5
