# WORK LOG — 자율 개발 진행 기록

형식: 시각 / 작업 / 결과

## 세션 1 — 기반 구축
- 프로젝트 골격(config/모델/파이프라인/CLI), 공간 발굴(네이버 수집 골격·제외필터·점수 프롬프트), 엑셀 출력, dry-run.
- 결과: `run --dry-run` 동작, 테스트 4개 통과.

## 세션 2 — 수요예측 + 컨셉
- demand 서브시스템(출생→12개월 시프트 수요·매출, 실적 보정, 광고 타이밍).
- concept 서브시스템(컨셉 카테고리 분류 + 제안서 12필드).
- 결과: `forecast --dry-run` 동작, 테스트 12개 통과. 커밋 완료.

## 세션 3 — 시장 인텔리전스 MVP (밤샘 자율)
목표: 목업/CSV 우선, 키 없이 전 구간 실행. 웨딩 성수기 로직 + 공급부족 예측 + 기회 리포트 + CSV 출력.
진행 내역은 아래에 계속 추가.

- [완료] 프로세스 문서(TODO/WORK_LOG) 작성.
- [완료] 공간 분석 모델 확장: 접근성·야외활용·우천대체·촬영포텐셜·프라이빗 행사 가능성.
- [완료] **목업/오프라인 모드**: `data/sample_candidates.csv`(수수밀소 포함 15곳) + CSV 수집기
  + 규칙기반 분석기(`analyze/heuristic`) + 규칙기반 점수기(`score/heuristic`). API 키 없이 전 구간 실행.
- [완료] `scoring.yaml` 가중치를 실제 점수 계산에 연결(기존엔 미사용이던 설정 활성화).
- [완료] **웨딩 성수기 로직**(`config/demand.yaml` seasonality): 성수기(3-5,9-11월)에
  돌잔치 고객이 숨은 공간으로 이동 → hidden_share 상승 → 숨은 공간 가치/수요 증가.
- [완료] **공급부족 예측**(`demand/supply`): 숨은공간 수요 vs 수용량 → 부족 + 확보추천 유형.
- [완료] **지역별 기회 리포트**(`demand/opportunity`): 부족 큰 지역 우선, 성수기 집중 여부, A등급 발굴수 연동.
- [완료] **CSV 출력 전면화**(`export/csv_out`): 공간DB / A등급 / 제안서 / 수요예측 / 광고계획 / 공급부족 / 기회.
- [완료] 컨셉 카탈로그에 의자 스타일·포토존 아이디어 추가(요구 레퍼런스 항목 충족).
- [완료] CLI에 **`intel` 통합 명령**(공간발굴+수요+공급+기회 한 번에).
- [완료] 테스트 19개 통과(제외/수요시프트/광고타이밍/보정/컨셉/성수기공급/기회/오프라인파이프라인).
- [완료] `.gitignore`에서 목업 입력 CSV 추적 예외 처리.

### 검증 결과 (목업)
- `intel`: 공간 13건 분석(웨딩홀 2곳 제외), A등급 2건(수수밀소 포함), 7종 리포트 생성.
- 성수기 로직 정상: 경남/경북 부족이 성수기 월에 집중 → "성수기 대비 선확보" 권고 출력.

### 다음 작업(키 필요/보류) → TODO.md 참조
- KOSIS·네이버·Claude·구글시트 실연동, 혼인건수/인구 추가 변수, 사진 기반 컨셉 분류.
